{{- if .Values.secret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hard-fixture.fullname" . }}
type: Opaque
stringData:
  password: {{ .Values.secret.stringData.password | quote }}
{{- end }}
